{
  "name": "boses-ko-trabaho-ko",
  "version": "1.0.0",
  "description": "AI-powered job recommendation platform for Filipino graduates",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "init": "npm install && npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client --production=false"
  },
  "keywords": [
    "job-recommendation",
    "ai",
    "career-guidance",
    "philippines",
    "react",
    "nodejs"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
